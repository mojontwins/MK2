// init drops

/* Drops are as follows:
	x1, y1 = initial position
	my = drop speed
	x, y = position
	mx = state: 0 fall, 1, 2, 3 hit
	x2 = subframe counter (hit)
*/

baddies [enoffsmasi].mx = 0;
baddies [enoffsmasi].x2 = 0;
